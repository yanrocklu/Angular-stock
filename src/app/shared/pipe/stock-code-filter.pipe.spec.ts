import { StockCodeFilterPipe } from './stock-code-filter.pipe';

describe('StockCodeFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new StockCodeFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
